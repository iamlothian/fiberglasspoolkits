import { ShellFilterPipe } from './shell-filter.pipe';

describe('ShellFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShellFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
